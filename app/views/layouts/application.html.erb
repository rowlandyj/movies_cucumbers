<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Movies Cucumbers" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Movies & Cucumbers" %>">
  <link href='http://fonts.googleapis.com/css?family=Hanuman:400,700' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield(:head) %>
</head>
<body class="<%= controller_name %> <%= action_name %>">
  <div class="navbar navbar-fixed-top">
    <nav class="navbar-inner">
      <div class="container">
 <%= render 'layouts/navigation' %>
   </div>
    </nav>
  </div>
        <%= render 'layouts/search-bar' %>
  <div id="main" role="main">
    <div class="container">
      <div class="span12">
      <%= render 'layouts/messages' %>
      <%= yield %>
    </div>
  </div> <!--! end of container -->
</div> <!--! end of #main -->
<%= render 'layouts/footer' %>
</body>
</html>
